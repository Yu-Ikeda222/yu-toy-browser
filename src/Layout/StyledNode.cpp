#include "StyledNode.h"

StyledNode::StyledNode(Node *node) {
    _node = node;
}

void StyledNode::calculateStyle(Rect rect) {
//    _rect.x = rect.x;
//    _rect.y = rect.y;
//    _rect.width = rect.width;
//    _width = _parent->_rect.width - _parent->_rect.x;
//    printf("%d\n",_children.size());
//    for(int i=0; i<_children.size(); ++i){
////        printf("chlid %s\n", _children[i]->_node->_node->name);
//        _height += _children[i]->_height;
//        _rect.height = _children[i]->_height;
//    }

}
